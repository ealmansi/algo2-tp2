\begin{interfaz}{Acuerdo}
\begin{iparamformales}{acuerdo}

\comment{\funcion{Copiar}{\param{in}{m}{$\alpha$}}{$\alpha$}
{true}
{\igres m}
{copy(m)}
{}
}

\textbf{\large se explica con:} Acuerdo

\end{iparamformales}

\operacion{NuevoAcuerdo}{\param{in}{pa}{paritaria}, \param{in}{pcj}{nat}}{acuerdo}{true}{esAlias(paritaria(acuerdo), pa)) \land (valor(pa) \igobs pcj)}{1}{La paritaria se guarda por referencia, y el porcentaje por copia}
\operacion{ObtenerParitaria}{\param{in}{ac}{acuerdo}}{paritaria}{true}{esAlias(res, paritaria(ac))}{1}{Se devuelve la referencia a la paritaria}
\operacion{ObtenerGremio}{\param{in}{ac}{acuerdo}}{gremio}{true}{esAlias(res, gremio(ac))}{1}{Se devuelve el gremio por referencia}
\operacion{ObtenerPorcentaje}{\param{in}{ac}{acuerdo}}{nat}{true}{valor(ac) \igobs res}{1}{Se devuelve el porcentaje por valor}

\end{interfaz}